# ⚡ התקנה מהירה - 5 דקות

## 🎯 מטרה
להפעיל את המערכת המלאה עם כל התכונות, כולל חיבור בנקים.

---

## ✅ שלב 1: הרץ SQL Migrations (חובה!)

### אופציה א': דרך Supabase Dashboard (מומלץ!)

1. **פתח את Supabase SQL Editor:**
   ```
   https://supabase.com/dashboard/project/tzhhilhiheekhcpdexdc/sql/new
   ```

2. **העתק והרץ את הקבצים הבאים לפי הסדר:**

   **קובץ 1: טבלאות בסיסיות**
   ```bash
   # העתק את כל התוכן מ:
   supabase/migrations/20260217212817_ffb675f2-c053-4081-8a1c-908d3479a2ee.sql
   # הדבק ב-SQL Editor ולחץ "Run"
   ```

   **קובץ 2: תכונות מתקדמות**
   ```bash
   # העתק את כל התוכן מ:
   supabase/migrations/20260220000000_advanced_features.sql
   # הדבק ב-SQL Editor ולחץ "Run"
   ```

   **קובץ 3: 100+ קטגוריות**
   ```bash
   # העתק את כל התוכן מ:
   supabase/migrations/20260221000000_initial_categories.sql
   # הדבק ב-SQL Editor ולחץ "Run"
   ```

3. **✅ סיימת!** כל הטבלאות נוצרו.

---

### אופציה ב': דרך CLI (למתקדמים)

```bash
cd /Users/yitzi/Downloads/family-finance-manager
npx supabase db push
```

---

## ✅ שלב 2: בדוק שהכל עובד

1. **רענן את האתר:** https://family-finance-manager-eta.vercel.app

2. **נסה להתחבר לבנק** - עכשיו לא תהיה שגיאה!
   - זה עדיין DEMO/סימולציה
   - אבל החיבור יעבוד ללא שגיאות

---

## 🏦 שלב 3: חיבור בנקים אמיתי (אופציונלי)

אם אתה רוצה חיבור **אמיתי** לבנקים (לא סימולציה):

### דרך Pepper (מומלץ לישראל)

1. **הירשם ל-Pepper:**
   ```
   https://www.pepper.co.il/developers
   ```

2. **קבל API Keys:**
   - Client ID
   - Client Secret
   - Redirect URI

3. **הוסף למשתני סביבה:**
   ```bash
   # ב-Vercel Dashboard:
   VITE_PEPPER_CLIENT_ID=your_client_id
   VITE_PEPPER_CLIENT_SECRET=your_secret
   VITE_PEPPER_REDIRECT_URI=https://family-finance-manager-eta.vercel.app/callback
   ```

4. **פרוס מחדש** - ב-Vercel Dashboard לחץ "Redeploy"

---

### דרך Salt Edge (אלטרנטיבה)

```
https://www.saltedge.com/signup
```

---

## 🎉 סיימת!

**עכשיו יש לך:**
- ✅ כל הטבלאות במסד נתונים
- ✅ 100+ קטגוריות מוכנות
- ✅ חיבור בנקים (DEMO) עובד ללא שגיאות
- ✅ כל 20 הפיצ'רים פעילים

---

## ❓ שאלות נפוצות

### שאלה: למה אני מקבל "שגיאה בחיבור הספק"?
**תשובה:** לא הרצת את ה-SQL Migrations. חזור לשלב 1.

### שאלה: איך אני יודע שה-Migrations רצו?
**תשובה:** לך ל:
```
https://supabase.com/dashboard/project/tzhhilhiheekhcpdexdc/editor
```
ובדוק שיש טבלאות: categories, transactions, accounts, וכו'.

### שאלה: החיבור לבנקים לא עובד אמיתי!
**תשובה:** נכון. זה DEMO. לחיבור אמיתי צריך API Keys (שלב 3).

### שאלה: כמה עולה Pepper/Salt Edge?
**תשובה:** 
- Pepper: תוכנית חינמית עד 100 משתמשים
- Salt Edge: תוכנית מפתחים חינמית

---

## 🆘 עזרה

**בעיה?** פתח Issue ב-GitHub:
```
https://github.com/Yklein888/family-finance-manager/issues
```

או שלח מייל:
```
support@finfamily.com
```

---

**נבנה ב-❤️ בישראל | FinFamily 2026**
